//Third Party Imports
import { Map, GoogleApiWrapper } from 'google-maps-react';

//First Party Imports
import { API_KEY } from "../env";


export const FMap = () => {
  return (
    <h1></h1>
  );
}

